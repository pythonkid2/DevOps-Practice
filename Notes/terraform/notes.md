# state file

It tracks the real-world infrastructure so Terraform can detect drift
  Mapping Terraform resources → real cloud resources
  Calculating what to create, update, or destroy
  Maintaining resource dependencies
